const api = require('./index')()
const run = async() => {
  const bucket = api.bucket({
    slug: 'next-merge-live-staging',
    read_key: 'crvax6wakPEGYgY3RBoAnVs1pgMJDx3hvORx3isg7KaOIy3jC7'
  })
  const query = {
    folder: "test"
  }
  try {
    const media = await bucket.getMedia({ query })
    console.log(media)
  } catch (err) {
    console.log(err)
  }
}
run();